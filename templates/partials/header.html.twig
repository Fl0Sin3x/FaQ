<header>
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <a class="navbar-brand" href="{{ path('app_home') }}">SinexOverFlo'w</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse"
                aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_home') }}">Home</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('question_list') }}">Questions</a>
                </li>
                ·
                <li class="nav-item active">
                    <a class="nav-link" href="#">Tags ?</a>
                </li>
            </ul>
            <div class="navbar-item-custom">
                {% if app.user %}
                    <a style="color: white"
                       href="{{ path('app_profile') }}"><strong>{{ app.user.firstname }}</strong></a> |
                    <a style="color: white" href="{{ path('app_logout') }} ">Deconnexion </a>
                {% else %}
                    <a style="color: white" href="{{ path('app_login') }}">Connexion</a> | <a
                        href="{{ path('app_register') }} ">Inscription</a>
                {% endif %}
        </div>
    </div>
</nav>
</header>
